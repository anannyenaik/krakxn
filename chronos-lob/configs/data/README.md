# Data Configs

Data configuration files define how local market data should be read, validated and
prepared for later research workflows.

Use this directory for source paths, symbols, venues, sampling parameters, timestamp
rules, schema validation options and order book reconstruction settings.

Do not store raw data, secrets, API keys or downloaded files here. Future configs
should document every data assumption needed to reproduce a dataset.
