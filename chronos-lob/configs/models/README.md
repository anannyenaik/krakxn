# Model Configs

Model configuration files define architecture choices and hyperparameters.

Use this directory for model families, input shapes, embedding sizes, optimisation
settings and checkpoint naming conventions once modelling phases begin.

Do not record benchmark claims here. Model configs should describe how a model is
constructed, not imply that it has been validated.
